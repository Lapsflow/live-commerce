# 핫픽스 2026-05-12: 권한 격리 + 사용자 정리 + 운영 흐름 안내

> 대표님 카톡 답변 후속 작업 (Q1: B 비활성화 / Q2: A 자기 센터 우선 / Q3: A 컨펌 유지)

## 요약

| # | 작업 | 영향 | 결과 |
|---|------|------|------|
| 1 | ONEWMS 위젯 권한 격리 | dashboard | MASTER 전용으로 |
| 2 | 자동 등록 미검토 뱃지 권한 격리 | products | MASTER 전용으로 |
| 3 | 테스트 계정 일괄 삭제 스크립트 | scripts | dry-run + apply 모드 |
| 4 | 사용자 비활성화 UI 명시화 | users | 액션 컬럼에 버튼 추가 |
| 5 | 상품 관리 센터 권한 격리 | products | 기본 탭 자기 센터, 본사는 열람 전용 |
| 6 | 발주 엑셀 업로드 WMS 안내 | orders/upload | 운영 흐름 안내 박스 + toast |

---

## 1. ONEWMS 위젯 → MASTER 전용

### 변경
- `app/(main)/dashboard/page.tsx`
  - `!isSeller` → `isMaster` 로 권한 조건 변경
  - ONEWMS 통계 API 호출도 MASTER일 때만 (불필요 호출 차단)

### 영향
- SUB_MASTER (센터관리자) 로그인 시 본사 ONEWMS 연동 상태 위젯이 보이지 않음
- 재고 충돌 12,692건 같은 본사 운영 정보 노출 해소

---

## 2. 자동 등록 미검토 뱃지 → MASTER 전용

### 변경
- `app/(main)/products/page.tsx`
  - `canReviewAutoCreated = isMaster` 추가
  - useEffect fetch 조건도 동일하게 적용
  - "자동 등록 N건 미검토" 버튼 표시 조건 변경

### 영향
- SUB_MASTER 상품 관리 화면에서 "자동 등록 73건 미검토" 노란 뱃지가 더 이상 보이지 않음
- 재고 초기화 버튼도 MASTER 전용 (`canResetStock = isMaster`)

---

## 3. 테스트 계정 일괄 삭제 스크립트

### 신규 파일
- `scripts/cleanup-test-accounts.ts`

### 사용법
```bash
# dry-run (목록만 확인)
pnpm tsx scripts/cleanup-test-accounts.ts

# 실제 삭제
pnpm tsx scripts/cleanup-test-accounts.ts --apply
```

### 보호되는 계정 (절대 삭제 안 됨)
- `master`
- `admin1`
- `seller1`, `seller2`, `seller3`

### 삭제 대상 패턴
- 이름/아이디에 "test", "E2E", "Validation", "수락테스트", "통합테스트", "MCP테스트", "Bcrypt테스트", "Phase{N}", "테스터", "변경테스터", "해시테스트", "로그인테스트" 등 포함
- 센터도 동일 패턴으로 정리

### 권장 실행 순서
1. 먼저 dry-run으로 삭제 대상 확인
2. 잘못 잡힌 계정이 있으면 알려주세요 (보호 목록 추가 가능)
3. `--apply` 로 실제 실행

---

## 4. 사용자 비활성화 UI

### 변경
- `app/(main)/users/page.tsx`
  - 액션 컬럼에 "비활성화" / "활성화" 버튼 명시 추가
  - 비활성화 시 확인 다이얼로그 (실수 방지)
  - 발주/방송 이력은 그대로 유지 (Soft Delete)

### 정책 (대표님 답변 Q1-B)
- 완전 삭제 안 함
- 비활성화 처리로 로그인만 차단
- 데이터 손실 없음 → 회계 감사 대응 안전
- 필요 시 활성화 버튼으로 복구 가능

---

## 5. 상품 관리 — 센터 권한 격리 (Q2-A)

### 변경
- `app/(main)/products/page.tsx`
  - SUB_MASTER 진입 시 기본 productTypeFilter = "CENTER" (자기 센터 제품 우선)
  - SUB_MASTER 에게는 "전체" 탭 숨김
  - 본사 탭 라벨: "본사 카탈로그 (열람 전용)"
  - 센터 탭 라벨: "우리 센터 제품"
  - 본사 탭 진입 시 안내 박스: "열람만 가능하며 수정 추가 삭제는 본사에서 진행합니다"

### 효과
- 센터 관리자는 자기 책임 영역과 본사 영역을 화면에서 명확히 구분
- 본사 제품을 실수로 수정할 위험 차단

---

## 6. 발주 엑셀 업로드 후 WMS 매칭 안내 (Q3-A)

### 변경
- `app/(main)/orders/upload/page.tsx`
  - 상단에 운영 흐름 안내 박스 추가 (3단계 설명)
  - 업로드 완료 toast 메시지에 컨펌 안내 추가

### 안내 내용
1. 엑셀 업로드 → 발주 본사 접수
2. 본사 컨펌 → ONEWMS 자동 동기화 (본사 제품)
3. 센터 제품은 해당 센터로 자동 발송

### 정책 (대표님 답변 Q3-A)
- 컨펌 단계 유지 (검수 가능)
- 운영자가 흐름을 명확히 알 수 있도록 안내 추가
- 추후 자동 매칭이 필요하면 컨펌 흐름을 자동화로 전환 가능

---

## 검증

- TypeScript: `tsc --noEmit --skipLibCheck` → 통과
- ESLint: 기존 프로젝트 패턴(`any`, useEffect deps)과 동일 — 회귀 없음
- 영향 파일 5개 (페이지 4 + 스크립트 1)

---

## 변경 파일 목록

```
M  app/(main)/dashboard/page.tsx
M  app/(main)/products/page.tsx
M  app/(main)/users/page.tsx
M  app/(main)/orders/upload/page.tsx
A  scripts/cleanup-test-accounts.ts
A  docs/HOTFIX_2026-05-12.md  (본 문서)
```

---

## 배포 후 운영 확인 (5개 화면)

1. SUB_MASTER 로그인 → /dashboard → ONEWMS 위젯 미노출 확인
2. SUB_MASTER 로그인 → /products → "자동 등록 N건 미검토" 뱃지 미노출 확인
3. SUB_MASTER 로그인 → /products → 기본 탭이 "우리 센터 제품"인지 확인
4. SUB_MASTER 로그인 → /products → "본사 카탈로그" 탭 클릭 → 안내 박스 노출 확인
5. MASTER 로그인 → /users → 액션 컬럼에 "비활성화" 버튼 노출 확인
6. MASTER 로그인 → /orders/upload → 상단 운영 흐름 안내 박스 확인

---

## 배포 후 권장 운영 액션

1. 테스트 계정 정리 스크립트 실행:
   ```bash
   pnpm tsx scripts/cleanup-test-accounts.ts            # dry-run
   pnpm tsx scripts/cleanup-test-accounts.ts --apply    # 실제 삭제
   ```
2. 신규 센터 계정으로 다시 로그인하여 본사 정보 노출이 없는지 직접 확인
