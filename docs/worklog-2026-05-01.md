# 작업일지 — 2026-05-01

## 작업 목표

**ADMIN Role 완전 제거 (Step 3~4)**
- 권한 모델: `MASTER / SUB_MASTER / ADMIN / SELLER` → `MASTER / SUB_MASTER / SELLER`
- `adminId` 자기참조 관계 (User→User, Order→User) 완전 삭제
- DB 스키마, 코드, 데이터 동시 정리

---

## 완료된 작업

### Step 3 마무리: 코드 정리 (잔여 adminId 참조 제거)

이전 세션에서 ~65개 파일 수정 완료, 이번 세션에서 나머지 파일 처리.

| 파일 | 변경 내용 |
|------|-----------|
| `app/api/users/[id]/route.ts` | select에서 `adminId: true` 제거, audit log `before` 필드에서 adminId 제거 |
| `app/api/stats/dashboard/route.ts` | seller ranking에서 `adminId`, `admin`, `adminName` 제거 |
| `app/api/proposals/[id]/route.ts` | GET/DELETE handler user select에서 `adminId` 제거 |
| `app/api/stats/seller/[id]/route.ts` | seller select에서 `adminId` 제거 |
| `app/api/stats/seller/analytics/route.ts` | seller select에서 `adminId` 제거 |
| `app/api/stats/admin/[id]/route.ts` | adminId 기반 → centerId 기반 조회로 전환 (변수명: adminId→managerId, admin→manager) |
| `types/order.ts` | Order interface에서 `adminId: string \| null` 제거 |
| `prisma/seed.ts` | admin1/admin2 role ADMIN→SUB_MASTER, adminId 참조 전부 제거 |
| `scripts/seed-audit-log-test.ts` | role 변경 로그 "ADMIN" → "SUB_MASTER" |

### Step 4: DB 스키마 마이그레이션

#### Pre-1: Neon DB 백업 확인
- Host: `ep-holy-firefly-a1vh7qba-pooler.ap-southeast-1.aws.neon.tech`
- Neon PITR (Point-in-Time Recovery) 활성 상태

#### Pre-2: 명시적 JSON 백업
- 스크립트: `scripts/backup-before-admin-removal.ts`
- 백업 위치: `backups/pre-admin-removal-2026-05-01T01-31-05-568Z/`

| 파일 | 내용 |
|------|------|
| `users.json` | 11명 (MASTER 1, ADMIN 2, SELLER 8) — 3.4KB |
| `orders-with-admin.json` | 0건 (adminId가 NULL이 아닌 주문 없음) |
| `audit-log-snapshot.json` | 864건 — 594KB |
| `metadata.json` | 타임스탬프, 카운트 등 — 376B |

#### Pre-3: 롤백 스크립트
- `scripts/rollback-admin-removal.ts` 생성
- DRY RUN 기본, `--confirm` 플래그로 실제 실행

#### 4-0: 사전 점검
- ADMIN role 사용자: 2명 (admin1, admin2 — 둘 다 isActive=false)
- adminId NOT NULL 사용자: 0명
- adminId NOT NULL 주문: 0건
- 마이그레이션 히스토리: 15개 전부 unapplied (drift) → `db push` 방식 결정

#### 4-0-1: ADMIN 사용자 역할 변환
- admin1, admin2의 role `ADMIN` → `SUB_MASTER` 변경
- AuditLog 기록 시 이슈 2건 해결:
  - `userRole: "SYSTEM"` → Role enum에 없음 → `"MASTER"` 사용
  - `userId: "system"` → FK 위반 → master 사용자 ID 사용
- 최종: ADMIN 0명, SUB_MASTER 2명

#### 4-1: Schema 변경 (`prisma/schema.prisma`)

```diff
enum Role {
  MASTER
  SUB_MASTER
- ADMIN
  SELLER
}

model User {
- adminId      String?
- admin        User?    @relation("AdminSellers", fields: [adminId], references: [id], onDelete: SetNull)
- sellers      User[]   @relation("AdminSellers")
- adminOrders  Order[]  @relation("AdminOrders")
- orders       Order[]  @relation
+ orders       Order[]
}

model Order {
- adminId  String?
- admin    User?  @relation("AdminOrders", fields: [adminId], references: [id])
}
```

#### 4-2: db push 실행

| 시도 | 결과 | 원인/조치 |
|------|------|-----------|
| 1차 | `--accept-data-loss` 필요 경고 | ADMIN enum 제거 시 데이터 손실 경고 |
| 2차 | **실패** `invalid input value for enum Role_new: ADMIN` | AuditLog 25건에 `userRole='ADMIN'` 잔존 |
| 조치 | AuditLog 25건 `ADMIN` → `SUB_MASTER` 변경 | `prisma.auditLog.updateMany()` |
| 3차 | **성공** `Your database is now in sync` | — |

#### 4-3: Prisma Client 재생성
- `npx prisma generate` v7.6.0 (154ms)
- generated `index.d.ts` 내 "ADMIN" 참조: 0건
- generated `index.d.ts` 내 "adminId" 참조: 0건

#### 4-4: 검증

**타입체크 / 빌드**
- `tsc --noEmit`: 3개 파일 에러 → 수정 후 0 errors
  - `app/api/orders/[id]/route.ts` — admin include 제거
  - `app/api/users/[id]/stats/route.ts` — admin relation 참조 제거
  - `scripts/backup-before-admin-removal.ts` — 실행 완료 stub로 교체
- `npm run build`: SUCCESS

**소스코드 grep**
- `"ADMIN"` 참조 (generated/backup 제외): 0건
- `adminId` 참조 (generated/backup 제외): 0건

**API 회귀 테스트 (로컬 dev 서버)**

| Endpoint | HTTP | 데이터 | adminId 필드 | ADMIN role |
|----------|------|--------|-------------|------------|
| GET /api/users | 200 | 11건 | 없음 | 없음 (MASTER:1, SUB_MASTER:2, SELLER:8) |
| GET /api/products | 200 | 1,126건 | 없음 | — |
| GET /api/broadcasts | 200 | 10건 | 없음 | — |
| GET /api/orders | 200 | 243건 | 없음 | — |
| GET /api/admin/audit-log | 200 | 870건 | 없음 | — |

---

## 데이터 무결성 요약 (Before / After)

| 항목 | Before | After |
|------|--------|-------|
| Users | 11명 | 11명 |
| Orders | 243건 | 243건 |
| Products | 1,126건 | 1,126건 |
| Broadcasts | 10건 | 10건 |
| AuditLog | 864건 → 870건 | 870건 (role 변환 로그 추가) |
| ADMIN 유저 | 2명 (비활성) | 0명 (SUB_MASTER로 변환) |
| Role enum | MASTER, SUB_MASTER, ADMIN, SELLER | MASTER, SUB_MASTER, SELLER |
| adminId 컬럼 | User/Order에 존재 (모두 NULL) | 삭제됨 |

---

## 미완료 / 다음 작업 (Step 5)

1. **Playwright 테스트 업데이트**
   - `playwright/.auth/admin.json` — admin1 계정 비활성화됨, auth setup 수정 필요
   - admin1/admin2 참조하는 테스트 시나리오 수정 또는 제거
   - SUB_MASTER 기반 테스트로 전환

2. **Vercel 배포**
   - 현재 DB는 이미 변경됨 (adminId 컬럼 삭제, ADMIN enum 제거)
   - 프로덕션 코드는 아직 이전 버전 → **빠른 배포 필요** (현재 배포된 코드가 없는 컬럼 참조 가능)

3. **최종 빌드 검증**
   - `npm run build` 후 Vercel 배포
   - 프로덕션 API 회귀 테스트

4. **SUB_MASTER centerId NULL 처리**
   - admin1/admin2가 SUB_MASTER로 변환되었으나 centerId가 NULL
   - centerId 없는 SUB_MASTER의 접근 권한 정리 필요

5. **메모리/문서 업데이트**
   - MEMORY.md에서 ADMIN 관련 내용 정리
   - Playwright 테스트 메모리 (admin auth) 업데이트

---

## 주의사항

- **긴급**: DB 스키마가 이미 변경되었으므로 프로덕션 배포를 빨리 해야 함. 현재 Vercel에 배포된 코드는 `adminId` 컬럼을 참조할 수 있어 에러 발생 가능.
- 롤백 필요 시: `scripts/rollback-admin-removal.ts` + Prisma schema git revert + `db push`
- 백업: `backups/pre-admin-removal-2026-05-01T01-31-05-568Z/`
